(define (problem p_wake_critical)
    (:domain question_2)
    (:objects
        vic - victim
        loc1 - location
        loc2 - location
        questioner - player
        answerer - player
    )

    (:htn
        :parameters ()
        :tasks (collaborate_critical_wake)
        :ordering ()
    )

    (:init
        (at vic loc2)
;        (positioned answerer loc1)
;        (lost answerer)
;        (marked vic) 
;        (unmarked vic)
    )
)
