(define (problem p_carry_stabilized)
    (:domain question_2)
    (:objects
        vic - victim
        loc1 - location
        loc2 - location
        questioner - player
        answerer - player
    )

    (:htn
        :parameters ()
        :tasks (collaborate_carry_stabilized)
        :ordering ()
    )

    (:init
        (healed vic)
;        (at vic loc2)
;        (positioned answerer loc1)
;        (lost answerer)
;        (marked vic) 
        (unmarked vic)
    )
)
