    (define
        (problem carry_Stabilized)
        (:domain question_2)
        (:objects
            qu - player
            an - player
            crit - critical
            loc - location
            mark - marker
        )

        (:htn
            :parameters ()
            :task (collaborate_carry_stabilized qu crit loc mark)
        )

        (:init
            (and (found crit loc)
                 (saved crit)
                 (is_marked crit))
        )
  )

