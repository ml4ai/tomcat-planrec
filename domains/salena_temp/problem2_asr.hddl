    (define
        (problem carry_Stabilized)
        (:domain question_2)
        (:objects
            vic - victim
            loc - location
        )

        (:htn
            :parameters ()
            :task (collaborate_carry_stabilized vic loc)
        )

        (:init
            (and (found vic loc)
                 (saved vic))
        )
  )

