    (define
        (problem carry_Stabilized)
        (:domain question_2)
        (:objects
            crit - critical
            loc - location
        )

        (:htn
            :parameters ()
            :task (collaborate_carry_stabilized crit loc)
        )

        (:init
            (and (found crit loc)
                 (saved crit))
        )
  )

