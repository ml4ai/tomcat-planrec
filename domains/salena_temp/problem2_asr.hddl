    (define
        (problem carry-Stabilized)
        (:domain question-2)
        (:objects
            qu - player
            an - player
            crit - critical
            loc - location
            mark - marker
        )

        (:htn
            :parameters ()
            :task (collaborate-carry-stabilized qu crit loc mark)
        )

        (:init
            (found crit loc)
        )
  )

