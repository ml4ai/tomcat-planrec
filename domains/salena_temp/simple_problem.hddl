(define (problem p_carry_stabilized)
    (:domain question_2)
    (:objects
        vic - victim
    )

    (:htn
        :parameters ()
        :tasks (and 
                    (request_marker_1 vic) 
                    (request_carrySaved_25 vic))
        :ordering ()
    )

    (:init
        (saved vic))
)
