(define (problem p_carry_stabilized)
    (:domain question_2)
    (:objects
        vic - victim
        loc1 - location
        loc2 - location
        plyr - player
    )

    (:htn
        :parameters ()
        :tasks (collaborate_carry_stabilized)
        :ordering ()
    )

    (:init
        (healed vic)
        (at vic loc1)
        (marked vic);uncomment to test m_34
    )
)
